query GetUserById($id: uuid!) {
  user(id: $id) {
    id
    allowedOrgs {
      orgId
    }
    defaultRole
    displayName
    email
    avatarUrl
    metadata
  }
  memberships(where: { userId: { _eq: $id } }) {
    orgId
    role
  }
  authUserRoles {
    role
  }
}
