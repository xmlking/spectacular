query OrgSettings($id: OrgIdFromSession!) @cache(policy: NetworkOnly){
  organizations_by_pk(id: $id) {
     settings {
      key
      value
    }
    settingsWithDefaults {
      key
      value
    }
  }
  setting_keys {
    key
    description
    allowed_values
    default_value
  }
}
