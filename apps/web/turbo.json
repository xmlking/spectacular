{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"env": [
				"VERCEL",
				"VERCEL_URL",
				"VERCEL_ENV",
				"BUILD_VERSION",
				"BUILD_TIME",
				"npm_package_version"
			]
		},
		"preview": {}
	}
}
