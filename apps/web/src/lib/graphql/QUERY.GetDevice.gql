query GetDevice($id: uuid!) {
	devicesByPk(id: $id) {
		id
		ip
		displayName
		description
		organization
		tags
		annotations
		version
		createdAt
		createdBy
		updatedAt
		updatedBy
		deletedAt
	}
}
