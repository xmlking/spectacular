mutation InsertDevicePool($poolId: uuid!, $deviceId: uuid!) {
	insertDevicePoolOne(object: { poolId: $poolId, deviceId: $deviceId }) {
		associationId: id
		poolId
		deviceId
		createdAt
		createdBy
	}
}
