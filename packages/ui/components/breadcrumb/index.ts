import Root from './breadcrumb.svelte';
import Item from './breadcrumb-item.svelte';
import Separator from './breadcrumb-separator.svelte';

export const Breadcrumb = { Root, Item, Separator };
export { Root as BreadcrumbRoot, Item as BreadcrumbItem, Separator as BreadcrumbSeparator };
